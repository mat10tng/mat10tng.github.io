---
id: "step-01-prompt-to-self-curating"
title: "Never repeat a prompt: building project-agnostic memory"
description: "eliminate repetitive prompting by establishing a persistent memory system."
pubDate: 2026-04-23
loop: "improve"
---
import Section from '../../components/layout/Section.astro';

<Section>
<div slot="insight">
## The Answer
**You can eliminate repetitive prompting and force your technical knowledge to compound across your entire portfolio by establishing an autonomous, project-agnostic memory system.**

Reliability comes from an architecture that performs maintenance by default. By defining structural rules once, you ensure that every session—on any project—initializes, curates, and persists its own engineering standards without manual intervention.
</div>
</Section>

<Section>
<div slot="insight">
## 1. Portfolio Architecture
Compounding requires a strict directory hierarchy. This ensures the model has a deterministic location to store and retrieve history for any project in your portfolio.
</div>

<div slot="technical">
**Unified Memory Structure**
```text
memory/
├── state.json (global health)
├── global/
│   └── lessons/ (portfolio standards)
└── project-x/
    ├── sessions/ (daily logs)
    └── lessons/ (project fixes)
```

**Setup (Linux / macOS)**
```bash
mkdir -p memory/global/lessons
```
</div>
</Section>

<Section>
<div slot="insight">
## 2. System Rules: The Auto-Run Logic
The system is directed by **System Rules** (e.g. `CLAUDE.md`, `.cursorrules`). These are not just instructions; they are the execution logic that forces the model to maintain the architecture autonomously.
</div>

<div slot="technical">
**Mandatory Bootloader Logic**
```markdown
# Project Standards
- **Initialization:** On boot, identify current project. If `memory/[project-name]` does not exist, run `/init-project` immediately.
- **Maintenance:** Check `memory/state.json`. If `last_distill` > 7 days, execute `/distill` before accepting goals.
- **Persistence:** On session completion, autonomously execute `/end-session`.
- **Role:** Architect directs; LLM executes and maintains.
```
</div>
</Section>

<Section>
<div slot="insight">
## 3. Autonomous Skills
The model uses three core skills to maintain the loop without being prompted.

**Initialize**: Scaffolds the environment for new projects.
**Capture**: Persists technical outcomes at the end of work.
**Graduation**: Identifies rules with portfolio-wide utility.
</div>

<div slot="technical">
**Skill: `/init-project`**
> "Scaffold the memory structure: `memory/[project-name]/sessions/` and `memory/[project-name]/lessons/`."

**Skill: `/end-session`**
> "Summarize technical decisions. Save to `memory/[project-name]/sessions/[date]-[goal].md`."

**Skill: `/distill`**
> "Review logs. Identify rules to graduate to `global/lessons/`. Update `memory/state.json`."
</div>
</Section>

<Section>
<div slot="insight">
## The System-Led Workflow
Execution follows a deterministic, autonomous cycle:
1. **Boot**: LLM scaffolds or synchronizes project context.
2. **Curation**: LLM graduates rules if the global state is stale.
3. **Execute**: Architect directs; LLM implements.
4. **Persist**: LLM logs the session automatically before exit.
</div>
</Section>

<Section>
<div slot="insight">
## 4. Before You Paste This
One missing detail: this does not work in a normal chat window.

The model needs to be running in a local coding tool or terminal agent. Not because the idea is technical, but because the system has to create folders, write markdown files, and update its own rules. If it cannot touch files, it can only describe the system back to you.
</div>

<div slot="technical">
**Choose your path**

**Path A: local tool**

1. Download and install a local AI coding tool or terminal-based agent.
2. Create or choose a folder where the memory system should live.
3. Open that folder inside the tool.
4. When the tool asks for permissions, allow file access for that folder.
5. If it asks for terminal or shell permission, allow it.
6. If you want the changes tracked, sign in to git or GitHub inside the tool.
7. Ask the model to create a file called `permission-test.md`.
8. If the file appears, delete it and paste the setup prompt below.

**Path B: web workspace**

1. Use a web AI workspace that has persistent files or projects, not a normal chat window.
2. Create a dedicated project, workspace, or folder for the memory system.
3. If the tool supports it, connect persistent storage such as GitHub, cloud documents, or file export.
4. Ask the model to create a file called `permission-test.md` inside that workspace.
5. Close and reopen the workspace. If the file is still there, paste the setup prompt below.
6. If the file disappears, use the manual version: ask the model to generate the files, then download or copy them into your own memory folder after each session.

The permission test matters. It proves the model can actually write somewhere durable before you trust it with the larger setup. If the workspace loses files when the chat ends, it is not a self-curating system yet. It is just a conversation.
</div>
</Section>

<Section>
<div slot="insight">
## 5. The Full Setup Prompt
If I were starting this from zero, I would not ask the model to "remember things better."

I would give it full access, then make the maintenance loop part of its job. The only catch: the model has to be running somewhere that can actually touch files. If it can only chat, it cannot build this.
</div>

<div slot="technical">
````text
You are running in a local AI coding tool or terminal-based agent.

Before you begin, verify that you have:

- access to the current project folder
- permission to create and edit files
- permission to run shell/terminal commands
- git access if this folder is tracked with git

If any of those are missing, stop and tell me exactly what permission or local tool access is missing.
Do not pretend to set up a local memory system from a chat-only environment.

You have full access to this machine: filesystem, shell, git, and the current working directory.

Your job is to set up a self-curating memory system that works across every project in this portfolio.
The system should let future LLM sessions inherit context, capture what happened, distill durable lessons,
and graduate useful rules from one project to the whole portfolio.

Do the setup yourself. Do not stop at a plan unless something is genuinely blocked.

Create this memory structure at the portfolio root:

memory/
- state.json
- global/
  - lessons/
- projects/
  - [project-name]/
    - sessions/
    - lessons/

Use the current repository folder name as the first project name.

Create or update the project bootloader file used by this coding tool.
Use AGENTS.md, CLAUDE.md, .cursorrules, or the closest equivalent for the environment.

The bootloader must include these rules:

- On startup, identify the current project from the working directory.
- If memory/projects/[project-name] does not exist, initialize it before doing project work.
- Before starting non-trivial work, read global lessons and the current project's recent lessons.
- During work, prefer existing lessons over repeated prompting.
- At the end of every meaningful session, write a session note.
- When the same lesson appears in multiple project notes, graduate it into memory/global/lessons/.
- Once a week, review old session notes and distill them into smaller durable lessons.
- Keep raw sessions searchable, but keep startup context small.
- The human sets goals. The LLM maintains the memory system.

Create three reusable commands or prompt snippets:

1. /init-project
   - Detect the project name.
   - Create memory/projects/[project-name]/sessions/.
   - Create memory/projects/[project-name]/lessons/.
   - Add a short README explaining what belongs there.

2. /end-session
   - Summarize what changed.
   - Capture decisions, bugs fixed, commands that worked, commands that failed, and lessons learned.
   - Save the note to memory/projects/[project-name]/sessions/YYYY-MM-DD-[short-goal].md.
   - If a lesson is reusable, also save it to memory/projects/[project-name]/lessons/.
   - Use the session note template below.
   - Update memory/state.json with last_session_at and session count.

3. /distill
   - Review recent session notes.
   - Remove duplicate or overly specific notes.
   - Merge repeated project lessons into clearer project lessons.
   - Promote cross-project lessons to memory/global/lessons/.
   - Use the project lesson and portfolio lesson templates below.
   - Update memory/state.json with last_distill_at.

Initialize memory/state.json with:

- created_at
- last_distill_at
- projects
- global_lesson_count
- session_count

Use these exact markdown templates when writing memory notes.

SESSION NOTE TEMPLATE
Path: memory/projects/[project-name]/sessions/YYYY-MM-DD-short-goal.md

```markdown
---
type: session
project: [project-name]
date: YYYY-MM-DD
goal: "[what we were trying to do]"
outcome: success | struggled | failed | abandoned
reusable: false
---

# [short goal]

## Goal
[One or two sentences describing the work.]

## What Changed
- [User-visible or system-visible change.]
- [Important file, rule, command, or workflow touched.]

## Decisions
- [Decision made and why.]

## Commands
Worked:
- `[command]`

Failed:
- `[command]` - [why it failed or what it taught us]

## Lessons
- [Specific lesson from this session.]

## Follow-Up
- [Optional next action.]
```

PROJECT LESSON TEMPLATE
Path: memory/projects/[project-name]/lessons/short-lesson-title.md

```markdown
---
type: lesson
scope: project
project: [project-name]
created_at: YYYY-MM-DD
source_sessions:
  - memory/projects/[project-name]/sessions/YYYY-MM-DD-short-goal.md
---

# [lesson title]

## Rule
[The reusable rule in one sentence.]

## Why
[The problem this prevents or the pattern it captures.]

## How to Apply
- [When to use it.]
- [What to do differently next time.]

## Boundaries
[When this rule does not apply.]
```

PORTFOLIO LESSON TEMPLATE
Path: memory/global/lessons/portfolio-rule.md

```markdown
---
type: lesson
scope: global
created_at: YYYY-MM-DD
promoted_from:
  - memory/projects/[project-name]/lessons/short-lesson-title.md
projects_seen:
  - [project-name]
---

# [portfolio rule]

## Rule
[The cross-project rule in one sentence.]

## Why This Graduated
[The repeated signal that made this bigger than one project.]

## How Future Sessions Should Use It
- [Startup behavior, review habit, or implementation constraint.]
- [What the LLM should check before acting.]

## Exceptions
[Cases where the project-local rule should override this.]
```

After setup, run /init-project for the current repository.
Then write one short verification note showing:

- which files you created or updated
- which project name you detected
- how a future session should load context
- what command should be run at the end of a session

Constraints:

- Do not delete or rewrite existing project files unless required.
- Do not store secrets, credentials, private transcripts, or customer data in memory.
- Prefer concise lessons over long logs.
- If a rule is not reusable, keep it project-local.
- If a lesson changes behavior across all projects, put it in global lessons.
````
</div>
</Section>

<Section>
<div slot="insight">
## Result
A self-curating portfolio. The LLM inherits its environment and maintains its own infrastructure automatically, ensuring your engineering standards grow across every project you touch.
</div>
</Section>
