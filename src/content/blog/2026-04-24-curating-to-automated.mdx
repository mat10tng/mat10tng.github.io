---
title: "self-curating -> automated"
description: "automating context management by connecting the Memory via MCP."
pubDate: 2026-04-24
loop: "improve"
---
import Section from '../../components/layout/Section.astro';

<Section>
<div slot="insight">
## Principle
Automation removes the human bookkeeping required to maintain a self-curating system. The Model Context Protocol (MCP) enables direct, autonomous LLM interaction with the Memory.

## Automation Toolset
- **`read_lessons`**: Retrieves standards autonomously.
- **`save_session`**: Records outcomes directly to project directories in Memory.
</div>

<div slot="technical">
```bash
# Setup automation interface
mkdir simple-interface
cd simple-interface
npm init -y
npm install @modelcontextprotocol/sdk
```
</div>
</Section>

<Section>
<div slot="insight">
## Interface Implementation
The interface script exposes the file system (Memory) to the LLM, allowing it to perform its own retrieval and logging without manual prompting.
</div>

<div slot="technical">
```javascript
// index.js (Simplified)
server.setRequestHandler(CallToolRequestSchema, async (request) => {
  if (request.params.name === "save_session") {
    const { project, content } = request.params.arguments;
    const dir = path.join(MEMORY_PATH, project, "sessions");
    await fs.mkdir(dir, { recursive: true });
    await fs.writeFile(path.join(dir, `${Date.now()}.md`), content);
    return { content: [{ type: "text", text: "Logged." }] };
  }
});
```
</div>
</Section>

<Section>
<div slot="insight">
## Result
The shift from a manual to an automated system. The LLM maintains the architecture autonomously, ensuring Memory is updated and lessons are applied without human intervention.
</div>
</Section>
