---
title: "manual -> automated"
description: "connecting vaults to models via MCP."
pubDate: 2026-04-24
loop: "improve"
---
import Section from '../../components/layout/Section.astro';

<Section>
<div slot="insight">
## Principle
Remove the friction of manual context transfer by enabling direct model interaction with the file system.

## Toolset
- **`read_lessons`**: Retrieves standards autonomously.
- **`save_session`**: Records outcomes directly to project directories.
</div>

<div slot="technical">
```bash
# Setup
mkdir simple-bridge
cd simple-bridge
npm init -y
npm install @modelcontextprotocol/sdk
```
</div>
</Section>

<Section>
<div slot="insight">
## Bridge Logic
Expose file system tools via `StdioServerTransport` for local, low-latency execution.
</div>

<div slot="technical">
```javascript
// index.js (Simplified)
server.setRequestHandler(CallToolRequestSchema, async (request) => {
  if (request.params.name === "save_session") {
    const { project, content } = request.params.arguments;
    const dir = path.join(VAULT_PATH, project, "sessions");
    await fs.mkdir(dir, { recursive: true });
    await fs.writeFile(path.join(dir, `${Date.now()}.md`), content);
    return { content: [{ type: "text", text: "Logged." }] };
  }
});
```
</div>
</Section>

<Section>
<div slot="insight">
## Outcome
Autonomous context management. A command like "Save a session log" writes directly to the vault without human bookkeeping.
</div>

<div slot="technical">
```json
// Example Configuration
{
  "mcpServers": {
    "bridge": {
      "command": "node",
      "args": ["/path/to/simple-bridge/index.js"]
    }
  }
}
```
</div>
</Section>
